# Source of truth for remote-backend/variables.tf
#   terraform apply -var-file=terraform.tfvars.example
#
# Creates gs://via-supervisor-tfstate (this agent's dedicated remote backend).

product_name = "via-supervisor"
project_id   = "freyr-ai"
region       = "us-east1"
