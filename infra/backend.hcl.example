# Copy to backend.hcl and replace placeholders, then:
#   terraform init -backend-config=backend.hcl
#
# State is isolated per product and environment in terraform-dev-agent.

bucket = "terraform-dev-agent"
prefix = "PRODUCT_NAME/ENVIRONMENT"
