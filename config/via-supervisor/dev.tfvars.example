# Optional product overlay for via-supervisor in dev.
# Pipeline: -var-file=../config/<product>/<env>.tfvars.example
# product_name and environment still come from ADO parameters.

buckets = {
  bucket = {
    force_destroy = true
  }
}

secret_keys = [
  "app-config",
]
